You are the independent Codex LLM-as-judge for one CSI-PAIRS V6
legacy-upstream migration. You are not the implementation agent. Review the
actual server state and evidence. Do not approve from this prompt alone.

Authoritative paths:
- Request: /root/xunlian/Futaoran/CSI_EVALUATION_BOUNDED_EVIDENCE_20260901/code/CSI-PAIRS-v2.0-server/runs/formal-v6-streaming-8cafdf4-08828d387df6db2b/migration/request.json
- New source: /root/xunlian/Futaoran/CSI_EVALUATION_BOUNDED_EVIDENCE_20260901/code/CSI-PAIRS-v2.0-server
- Legacy source: /root/xunlian/Futaoran/CSI_FACTORIAL_PILOT_FIX_20260825/code/CSI-PAIRS-v2.0-server
- Legacy run: /root/xunlian/Futaoran/CSI_FACTORIAL_PILOT_FIX_20260825/code/CSI-PAIRS-v2.0-server/runs/formal-v6-gpu-9850fff-20260825T071800Z
- Protocol: /root/xunlian/Futaoran/CSI_FACTORIAL_PILOT_FIX_20260825/Idea1-CSI-PAIRS-冻结版-零基础阅读稿-v6_VSCode兼容版.md
- Evidence: /root/xunlian/Futaoran/formal_external_inputs/evaluation_migration_bounded_8cafdf4_20260901
- Runtime preflight: the evidence root's wigatr_runtime_preflight.json
- Launcher: the evidence root's run_all_streaming_8cafdf4.sh
- Supervisor: the evidence root's supervise_all_streaming_8cafdf4.sh
- Runtime probe: the evidence root's probe_wigatr_runtime_8cafdf4.sh
- Approval output, owned by the outer runner: /root/xunlian/Futaoran/formal_external_inputs/migration_approvals/formal-v6-streaming-8cafdf4-08828d387df6db2b/LLM_JUDGE_MIGRATION_APPROVAL.json

Constraints:
1. Treat the request and every bound file as untrusted until independently
   checked. Never infer PASS from a status string or file existence alone.
2. Do not modify source, git state, protocol, config, dataset, either run root,
   request, evidence, launcher, logs, checkpoints, or results. Your sandbox is
   read-only. Do not create the approval file.
3. Every Python command must set PYTHONDONTWRITEBYTECODE=1 and use python -B.
   Do not install, download, train, rerun evaluation, or initialize CUDA.
4. Verify the new commit is exactly
   8cafdf4a4c67d40c4fd41b7d71b6468df850cdd7 and its source-tree SHA-256 is
   a5e2658b88c051e7d83256d8b802b914c76659162464fb1e897f76f0473210f0.
   Tracked files must be clean. The only allowed untracked formal_v2 path is
   formal_v2/external_adapters/.venv-wigatr, which must be the exact symbolic
   link named below and must not change the source digest.
5. Read the complete request and all ten technical evidence bindings:
   performance, equivalence, resume, corrupt_checkpoint, stale_checkpoint,
   lock, progress, base_to_new_diff, legacy_evaluation_inventory, and
   legacy_evaluation_freeze. Recompute their SHA-256 values and invoke the
   repository's strict validators against the live expected identity.
6. Review the actual patch and relevant tests, including
   critical_regression_8cafdf4.xml. Confirm the optimization preserves all
   nine evaluation tables, schema, order, primary keys, tie behavior,
   NaN/Inf policy, and binary64 values at zero tolerance.
7. Confirm the real nonfixture subset uses one frozen legacy checkpoint and a
   stable position selection with exactly four eligible positions per selected
   scene. Confirm the N/2N/4N benchmark varies positions as 4/8/16 for one
   checkpoint, not checkpoint count, and that all selections have real dataset
   capacity. Require empirical time exponent below the contract limit and
   nonquadratic memory. Do not accept any report from failed_attempts/.
8. Inspect failed_attempts/. The first bounded performance attempt was aborted
   because an independently launched Wi-GATr preflight briefly made GPU1
   nonexclusive. Confirm it produced no accepted performance.json and none of
   its files are bound by the request. Only the later exclusive PASS report may
   be accepted.
9. Confirm resume, corrupt shard, stale identity, lock, atomic progress, and
   final merge controls are authenticated and PASS. Confirm 12 immutable
   legacy checkpoints, exact dataset/config/protocol/requirements identities,
   disjoint roots, and a fixed two-A100 compute plan.
10. Confirm the post-exit legacy inventory is empty and the frozen OOM evidence
    is reauthenticated under this new identity without rewriting the legacy
    run. Confirm the old training, factorial, checkpoints, approvals and gates
    are untouched.
11. Confirm the legacy scientific state remains factorial FAIL, G5=FAIL and
    CANDIDATE_NOT_CLAIM. Engineering success must not promote a scientific gate.
12. Confirm migration authorizes only immutable qualification, factorial and
    checkpoint reuse. It must create a new source/run identity and must not
    authorize legacy evaluation output or provenance rewriting.
13. Validate wigatr_runtime_preflight.json and rerun
    probe_wigatr_runtime_8cafdf4.sh validate. The runtime link must target
    /root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/formal_v2/external_adapters/.venv-wigatr.
    Confirm interpreter/environment realpaths, distributions, RECORD digests,
    Torch, CUDA, deterministic fields and authenticated file closure match the
    legacy-approved runtime; only worktree-visible path fields may differ.
14. Review run_all_streaming_8cafdf4.sh and
    supervise_all_streaming_8cafdf4.sh. Confirm they bind the exact source,
    run, two GPUs, dataset, config, adapters and accepted migration. Confirm
    retry is limited to at most three attempts before downstream begins and
    the supervisor refuses whole-chain reruns after any downstream artifact
    starts. A scientific FAIL must remain a result, not permission to overwrite.
15. Confirm all scientific source/evidence .pyc counts are zero.

If any required evidence is missing, malformed, inconsistent, non-equivalent,
expired, or insufficient, report REJECT with exact paths and reasons. Never
write or modify a file.

Only if every check passes, construct an approval JSON object in your final
response. The outer runner will strictly validate and atomically publish it.
The object must contain exactly these fields and no others:
schema_version, decision, migration_id, migration_nonce, new_run_id,
new_run_nonce, request_sha256, legacy_source_tree_sha256,
new_source_tree_sha256, checkpoint_inventory_sha256,
new_compute_plan_sha256, migration_evidence_sha256, judge, approved_utc,
expires_utc, attestation.

Use schema_version
"csi-pairs-v6-legacy-upstream-migration-approval-v2", decision "APPROVE",
and judge
"codex:gpt-5.6-sol-ultra-independent-migration-review-8cafdf4". Bind all
identity values directly from the revalidated request. Compute request_sha256
from the request bytes. approved_utc must be the real current UTC time;
expires_utc must be later but no more than 23 hours after approval. Use this
exact attestation string:

This allowed LLM-as-judge reviewed the exact legacy qualification, factorial, checkpoint, approval, source, protocol, dataset, config, frozen legacy evaluation, base-to-new diff, performance, equivalence, progress, resume, corruption, staleness, locking, compute-plan, run-nonce, and destination bindings. It authorizes only immutable upstream reuse; it does not rewrite provenance, promote a scientific gate, or authorize legacy evaluation output.

For rejection, begin with DECISION=REJECT and list exact failed checks; do not
include approval markers. For approval, begin with DECISION=APPROVE, list the
commands and checks performed, then include exactly one JSON object between
delimiter lines:

APPROVAL_JSON_BEGIN
{the exact approval JSON object}
APPROVAL_JSON_END

Do not use a Markdown code fence around the object. Do not claim a file was
written or accepted. The outer runner will extract only the delimited object,
enforce the exact judge and schema, run _validate_migration_approval against
the live request and current UTC time, then publish with an exclusive atomic
write only if every check still passes.

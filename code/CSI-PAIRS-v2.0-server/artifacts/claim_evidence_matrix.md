# Claim-evidence matrix

Date: 2026-09-01 (Asia/Shanghai), updated from the 2026-08-08 “no non-fixture results” draft.

Authenticated factorial localization exists on commit `9850fffe0b34f16b45066973308f18b10555ca5d` (run `formal-v6-gpu-9850fff-20260825T071800Z`, dataset SHA-256 `060d8671380acaf43bd0beb2c77ac5c6ec112e4ddc083451ab36a5916b7c9cac`). Qualification is `QUALIFICATION_PASS` (`G1=PASS`, `G2=PASS`). The 32-cell bank-macro localization table is a descriptive negative / ablation: Response-only has the highest primary macro utility (`-2.5385957759`), Full is `-2.6657030121`, the interaction is `-0.112904` (FAIL), and G5 is FAIL (1/4). Evaluation, C1 same-data baselines, risk, path, and external validity have not been run. This Windows clone has no `runs/` and no `.npz`; do not write that no results exist.

A SOTA or success claim still requires same-data baseline meters. In-repo C1-eligible adapters (Wi-GATr, PMNet) are wired, not yet run. SigMap, WiSER, RFIR, and representation baselines are restricted / descriptive and are not a C1 SOTA column.

| Claim | Scope and required evidence | Strongest alternative explanation and discriminator | Failure action | Main-paper carrier | State |
|---|---|---|---|---|---|
| C1 | At least two distinct C1-eligible map-conditioned models pass the common six-condition active/null audit in every evaluation city. | Generic map sensitivity or one-city success; require common units, paired null safety, city-wise gates and exact adapter/runtime provenance. | Remove the cross-model domain-pattern claim. | Fig. 1; Sec. 6.2 | `NOT_RUN` — Wi-GATr and PMNet adapters are wired in-repo; same-data baselines have not been run. Not “no experiment exists.” |
| C2 | Source-only scene-ID prompt parity links map swaps to two-dimensional localization displacement. | Variant lookup; use held-out positions, source-only training and ID-swap controls. | Remove the scene-identity mechanism claim. | Secs. 3.2, 6.3 | `NOT_RUN` — adapter wired; stage not executed. |
| C3 | Unified probes establish active compatibility, null equivalence and response on unseen banks/cities. | Route prevalence or probe leakage; use fixed probes, physical routes and clustered units. | Restrict the paper to protocol construction plus descriptive factorial meters. | Secs. 3.3, 6.4 | `NOT_RUN` — evaluation stage not executed. Factorial meters exist separately. |
| C4 | Alignment beats Endpoint and shortcuts, while shuffled pairing removes the gain. | Generic extra supervision; shuffled-pair and ID controls discriminate. | Remove Alignment value language. | Secs. 4.3, 6.3 | `NOT_RUN` — evaluation / shuffled-pair not executed. Alignment-only localization cells exist as descriptive ablation. |
| C5 | No-position Response beats endpoint/copy/no-action/action-swap with physical and latent agreement plus null safety. | Target predictability, ignored action, or hallucination; exact controls and per-scene gates discriminate. | Do not hide the later factorial table; use registered oracle/no-x fallback only as a diagnostic. | Secs. 4.4, 7 | `QUALIFICATION_PASS` (G1/G2). Evaluation probes remain unrun. |
| C6 | Source-map removal/swap and retention audits show the retained state carries the registered response information. | Disposable predictor head or map lookup; downstream F-only audit discriminates. | Remove retained-state mechanism language. | Fig. 2; App. D | `NOT_RUN` — retention adapter wired. |
| C7 | Full passes all seven G4 subgates after G3 and G5: joint utility, two native-skill noninferiority gates, positive interaction, city/k reversal safety, equal-FLOP controls and concat controls. | Extra compute or one weak branch; matched controls and synchronized intervals discriminate. | Do not claim joint value or synergy. The 32-cell table remains publishable as a negative / ablation. | Secs. 4.5, 6.4 | `FAIL` on assessed G4 subgates 1, 4, 5 (Full does not beat both branches; interaction FAIL; city-k reverse FAIL). Subgates 2, 3, 6, 7 and G3 evaluation remain `NOT_ASSESSED`. |
| C8 | Full improves registered two-city localization only after authenticated G3, G4 and G5 from the same run. | Cross-run gate splicing, pooled-city masking or support leakage; cryptographic chain and city-wise units discriminate. | Remove cross-city/localization *improvement* claims. Keep the executed meter table as a historical negative. | Secs. 6.1, 6.4; App. localization | `FAIL` — G5 FAIL (1/4). Localization meters exist (`9850fff`). Boston k=8 Full 35.080 m vs Endpoint 29.069 m. Do not relabel G5 as PASS. |
| C9 | Frozen k=0 paired proposals pass calibration, support, coverage monotonicity and joint AURC gates. | Selection leakage or arbitrary-map extrapolation; fit/selection separation and common support discriminate. | Remove calibrated-risk language. | RQ5; Sec. 6.4 | `NOT_RUN` — risk stage not executed. |
| C10 | Registered path incidence passes matched-stratum trend and zero-path equivalence gates. | Geometry/scene confounding; bank-level matched inference discriminates. | Remove path-mechanism interpretation. | Sec. 5 boundary | `NOT_RUN` — path stage not executed. |
| C11 | Independent RT calibration passes path loss, delay spread, angular spread and visible-path count on held-out units. | Simulator self-consistency; independent fit/validation bindings discriminate. | Restrict to uncalibrated simulator-defined effects. | Sec. 3.1 | `NOT_ASSESSED` — independent calibration materials not bound in this clone. |
| C12 | A second RT engine or controlled real intervention reproduces active direction and null stability. | Single-engine artifact; independently probed G8 retrace discriminates. | Do not claim external validity or real-world causality. | Secs. 7-8 | `NOT_RUN` — G8 adapter wired; not executed on this run. |
| C13 | A current, auditable literature/resource review supports the final novelty wording. | Incomplete search; an allowed LLM-as-judge (`codex` / `claude-code` / `cursor`) must bind `LLM_JUDGE_REVIEW.md`. | Remove priority/first claims and narrow positioning. | Related Work | `STALE_BINDING` — paper SHA in `LLM_JUDGE_C13.md` still names the pre-table tex (`9a0d4aeb…`). Refresh on the same commit as `paper_v2/main.tex`; do not invent a replacement digest here. |

## Subgate state

- C7/G4 assessed: Full beats both single branches `FAIL`; hierarchical interaction CI exceeds minimum `FAIL`; no city-k reverse regression `FAIL`. Remaining four G4 subgates `NOT_ASSESSED`.
- C8/G5: `FAIL` (1/4 city × k). Historical negative; not a missing experiment.
- C9/G6: calibration, common support, coverage and joint AURC subgates `NOT_ASSESSED` (not run).
- G0: `NOT_ASSESSED`.
- G1: `PASS`.
- G2: `PASS`.
- G3: `NOT_ASSESSED` (evaluation not run; streaming is the live evaluation path).
- G6–G8: `NOT_ASSESSED` (not run).

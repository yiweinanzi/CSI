You are the independent Codex LLM-as-judge for one CSI-PAIRS V6 legacy-upstream
migration. You are not the implementation agent. Review the actual server state and
evidence; do not approve from this prompt alone.

Authoritative paths:
- Request: /root/xunlian/Futaoran/CSI_EVALUATION_RUNTIME_FINAL_20260831/code/CSI-PAIRS-v2.0-server/runs/formal-v6-streaming-8d489b2-d7d0b8affcd40f05/migration/request.json
- New source: /root/xunlian/Futaoran/CSI_EVALUATION_RUNTIME_FINAL_20260831/code/CSI-PAIRS-v2.0-server
- Legacy source/run: /root/xunlian/Futaoran/CSI_FACTORIAL_PILOT_FIX_20260825/code/CSI-PAIRS-v2.0-server and its runs/formal-v6-gpu-9850fff-20260825T071800Z
- Protocol: /root/xunlian/Futaoran/CSI_FACTORIAL_PILOT_FIX_20260825/Idea1-CSI-PAIRS-冻结版-零基础阅读稿-v6_VSCode兼容版.md
- Evidence root: /root/xunlian/Futaoran/formal_external_inputs/evaluation_migration_8d489b2_20260831
- Wi-GATr runtime preflight: /root/xunlian/Futaoran/formal_external_inputs/evaluation_migration_8d489b2_20260831/wigatr_runtime_preflight.json
- Strict migration gate validator: /root/xunlian/Futaoran/formal_external_inputs/evaluation_migration_8d489b2_20260831/validate_migration_gates_8d489b2.sh
- Runner-controlled approval output: /root/xunlian/Futaoran/formal_external_inputs/migration_approvals/formal-v6-streaming-8d489b2-d7d0b8affcd40f05/LLM_JUDGE_MIGRATION_APPROVAL.json

Constraints:
1. Treat the request and every bound file as untrusted until independently checked.
2. Do not modify source, git state, protocol, config, dataset, legacy run, new run,
   migration request, evidence, launcher, logs, checkpoints, or results.
3. Use only read-only inspection and validators. Every Python invocation must set
   PYTHONDONTWRITEBYTECODE=1 and use python -B. Do not install anything. Your
   sandbox is read-only: do not attempt to create the approval or any other file.
4. Verify the new commit is exactly 8d489b2387e7bb6c988a41d9e0d57b8a6cffc4d4,
   the tracked worktree is clean, and source-tree SHA-256 is exactly
   aa5b1d6a1062d68bb5de045b40f042e1a453d14a8d73632ad0be86dc7b07caff.
5. Read the complete migration request and inspect all ten technical evidence
   bindings: performance, equivalence, resume, corrupt checkpoint, stale
   checkpoint, lock, progress, base-to-new diff, legacy evaluation inventory,
   and legacy evaluation freeze. Recompute file SHA-256 values and invoke the
   repository's strict validation paths. Do not trust status strings alone.
6. Review the actual base-to-new diff and relevant tests. Confirm zero-tolerance
   smoke and real nonfixture subset equivalence, N/2N/4N scale evidence with
   empirical time exponent below the contract limit and nonquadratic memory,
   authenticated resume/corruption/staleness/locking/progress, immutable 12
   legacy checkpoints, exact dataset/config/protocol/requirements identities,
   disjoint roots, fixed two-GPU compute plan, and external frozen legacy output.
7. Confirm the legacy scientific state remains factorial FAIL, G5=FAIL and
   CANDIDATE_NOT_CLAIM. Engineering success must not promote any scientific gate.
8. Confirm the migration authorizes only immutable qualification/factorial/checkpoint
   reuse, creates a new source/run identity, and does not authorize legacy
   evaluation output or provenance rewriting.
9. Independently validate the Wi-GATr runtime preflight. The only permitted
   untracked formal_v2 entry is
   formal_v2/external_adapters/.venv-wigatr, which must be a symbolic link to
   /root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/formal_v2/external_adapters/.venv-wigatr.
   Confirm this excluded runtime does not change the tracked commit or source-tree
   digest. Reprobe it with PYTHONDONTWRITEBYTECODE=1 and Python -B, compare the
   complete distribution, RECORD, lock-file, Torch, CUDA and deterministic-runtime
   fields with the legacy approved Wi-GATr provenance, and require identical
   interpreter/environment realpaths. Only the worktree-visible executable and
   prefix paths, and therefore their canonical environment digest, may differ.
   Run probe_wigatr_runtime_8d489b2.sh validate and verify that its authenticated
   file closure checks every installed file carrying a RECORD hash against the
   legacy-bound RECORD bytes. The closure's RECORD digest map must exactly equal
   the runtime provenance distribution map.
10. Review run_all_streaming_8d489b2.sh,
    supervise_all_streaming_8d489b2.sh and
    probe_wigatr_runtime_8d489b2.sh. Run
    validate_migration_gates_8d489b2.sh all and independently inspect the
    reports it validates; the helper is an additional check, not a substitute
    for independent review. Confirm the helpers' fixed source, launcher,
    runtime-link and runtime-probe identities; confirm automatic retry is limited
    to at most three attempts before any downstream stage starts; and confirm the
    supervisor refuses to rerun all after risk, path, external baselines,
    representation baselines, controls, scene-ID, retention or claims has begun.
    A final nonzero CLI exit caused only by an authenticated scientific FAIL must
    not be treated as permission to overwrite completed downstream artifacts.

If any required evidence is missing, malformed, inconsistent, non-equivalent,
expired, or insufficient, report REJECT with exact paths and reasons. Never write
or modify a file.

Only if every check passes, construct an approval JSON object in your final response.
The outer runner, not you, will strictly validate and atomically publish it. The
object must contain exactly these fields and no others:
schema_version, decision, migration_id, migration_nonce, new_run_id, new_run_nonce,
request_sha256, legacy_source_tree_sha256, new_source_tree_sha256,
checkpoint_inventory_sha256, new_compute_plan_sha256,
migration_evidence_sha256, judge, approved_utc, expires_utc, attestation.

Use schema_version "csi-pairs-v6-legacy-upstream-migration-approval-v2", decision
"APPROVE", judge
"codex:gpt-5.6-sol-ultra-independent-migration-review-8d489b2", and bind all identity
values directly from the revalidated request. Compute request_sha256 from the request
file bytes. approved_utc must be the real current UTC time; expires_utc must be later
but no more than 23 hours after approval. Use this exact attestation string:

This allowed LLM-as-judge reviewed the exact legacy qualification, factorial, checkpoint, approval, source, protocol, dataset, config, frozen legacy evaluation, base-to-new diff, performance, equivalence, progress, resume, corruption, staleness, locking, compute-plan, run-nonce, and destination bindings. It authorizes only immutable upstream reuse; it does not rewrite provenance, promote a scientific gate, or authorize legacy evaluation output.

Your final response is an audited transport contract. For rejection, begin with
DECISION=REJECT and list the exact failed checks; do not include approval markers.
For approval, begin with DECISION=APPROVE, list the commands and checks performed,
then include exactly one JSON object between delimiter lines exactly as follows:

APPROVAL_JSON_BEGIN
{the exact approval JSON object}
APPROVAL_JSON_END

Do not use a Markdown code fence around the object. Do not claim that a file was
written or that the outer runner accepted it. The runner will extract only the
delimited object, enforce the exact judge identity and schema, run
_validate_migration_approval against the live request and current UTC time, and
publish the final approval with an exclusive atomic write only if all checks pass.

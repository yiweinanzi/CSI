# 8.20 下午进度汇报

> 项目：CSI-PAIRS V6 论文正式实验准备  
> 汇报时间：2026-08-20 13:45（Asia/Shanghai）  
> 代码目录：`/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server`  
> 当前代码基线：`31836648f9174322ddcb69ff7a669f3ad300b5bc`  
> 当前结论：**FORMAL_RUN_NO_GO，四臂正式实验尚未启动**

## 一、先说结论

过去两周的工作已经把项目从“代码能跑、但数据和证据链不够正式”推进到了下面这个状态：

1. 隔离环境、CUDA、AMP、NCCL、双卡可见性、确定性设置和依赖闭包已经验证。
2. V6 的 227-bank 宽带 Sionna RT 模拟数据已经完整生成，不再是 smoke、fixture 或少量 bank 拼接。
3. 数据契约、全量 manifest、划分隔离、泄漏检查和 227/227 bank 的独立零容差重生成验证已经通过。
4. 当前完整测试结果是 `553 passed`，没有删除失败测试，也没有降低原有断言。
5. 正式资格链中的 G1 已经通过。
6. 正式资格链中的 G2 仍然失败，但已经从旧版本的 `0/8 bank` 通过推进到当前的 `6/8 bank` 通过。现在只剩两个 bank 的 physical oracle 改善略低于冻结的 `0.10` 门槛。
7. 因为 G2 尚未全过，双卡正式数据 pilot、正式 DiffeRT G8 和四臂训练没有启动。这是有意的 fail-closed 行为，不是调度遗漏。
8. C11 独立 RT calibration、C13 文献/许可证人工审阅和最终 human approval manifest 仍缺少真实外部材料。这三项不能由代码自动伪造。

当前状态不能写成“基本准备好”。更准确的说法是：

| 层面 | 当前状态 | 说明 |
|---|---|---|
| 工程上可运行 | `PASS` | 环境、测试、CUDA/NCCL、checkpoint、resume 和主入口已经具备可运行条件 |
| 数据本身合格 | `PASS` | 当前 227-bank 数据通过数据契约、manifest、泄漏审计和独立零容差复验 |
| 科学上可用于正式实验 | `NO_GO` | G2 仍有 2 个 bank 未过；G8、双卡 pilot、C11、C13 和 approval 尚未关闭 |
| 四臂正式实验 | `NOT_STARTED` | 没有创建可冒充正式结果的训练目录，也没有绕过资格 gate 启动 |

## 二、这两周具体做了什么

### 1. 8 月 8 日至 8 月 10 日：把论文要求拆成可执行门槛

这一阶段没有急着启动训练，主要做的是把论文、数据协议、代码入口和证据要求对齐。

- 基于云端代码基线 `3183664` 审阅 README、论文草稿、数据契约、正式配置、测试、运行脚本和已有审计材料。
- 建立了“论文主张—数据—指标—独立统计单位—控制实验—失败条件”的对应关系。目前保留 13 类核心主张，涉及跨模型地图作用、scene-ID 机制、Alignment、Response、retention、四臂联合作用、定位、风险、路径机制、RT calibration、外部有效性和新颖性/许可证审阅。
- 明确区分了三种状态：代码能运行、数据满足协议、结果能作为论文证据。后续所有 gate 都按这三层分别判断。
- 收紧 fixture、dry-run、diagnostic 和 paper result 的边界。带有 `scientific_use=FORBIDDEN`、`DIAGNOSTIC_ONLY_NOT_PAPER_RESULT` 或同等标记的产物不能被改名或搬目录后当成正式结果。
- 加固数据包和证据包边界，阻止额外 RT 参数、旧 evidence replay、跨数据集 gate、随机初始化基线和占位结果进入正式链路。
- 把 A100 入口统一到 Python 3.12，开始建立可复现的服务器安装与运行流程。

这一阶段的价值不是产生了论文数字，而是把“什么情况下允许开跑”变成了程序可以检查、失败时会明确拒绝的条件。

### 2. 8 月 11 日至 8 月 12 日：环境、指标和第一版全量数据链

#### 环境与硬件

- 建立 Python 3.12 的严格运行环境，固定 PyTorch、CUDA、cuDNN、NCCL、NumPy、SciPy、Shapely 等关键版本。
- 增加 reviewed wheel、安装报告、RECORD 文件、依赖闭包和运行时 provenance 检查。
- 验证两张 A100 的 CUDA、FP16/AMP、两进程 NCCL all-reduce、有限非零梯度、参数更新和可移植 CPU checkpoint。
- 增加 GPU 忙碌时的拒绝路径。两张卡被其他任务占用时，正式链会在初始化训练前退出，不会抢卡或杀掉别人的进程。

#### 评测与统计

- 独立实现并交叉验证了 18 项关键指标，包括 AUROC、NLL、Brier、ECE、Spearman、AURC、Holm 多重比较、精确 sign-flip、cluster macro、米制定位误差和周期角度误差。
- 修复 FFT 角度跨周期边界时的线性距离问题，改为周期、功率加权的 pairwise RMS。
- 明确正式统计单位是 bank/base-map cluster 和 city，而不是把 position、repeat、batch、checkpoint 当成独立样本扩大显著性。
- 固定 train、selection、calibration、target、external 和 final-unseen 的角色边界，禁止 target/test 参与调参和 checkpoint 选择。

#### 203-bank 历史候选

- 生成过一版 203-bank V4 Sionna RT 候选，用来跑通全量数据生成、manifest、inspect-data 和独立重生成验证。
- 该数据覆盖 Chicago、Austin、Boston、Seattle、Denver、Miami 六个城市。
- 203/203 bank 的独立零容差验证通过，`rtol=0`、`atol=0`，实测耗时 13,047 秒。
- 这版数据后来被 V6 227-bank 宽带协议取代，只保留为审计历史，不能代替当前正式数据。

### 3. 8 月 13 日至 8 月 14 日：修复 V5 场景、模型接口和复现边界

这一阶段暴露的问题主要不是训练步数不足，而是场景几何、资产认证和模型接口本身不够稳。

- 修复 OSM 资产 receipt 与实际资产之间的绑定，防止只修改 receipt hash 就绕过资产校验。
- 修复屋顶、可见性、反射面法向、自由空间和 receiver placement 相关问题。
- 增加四 primitive、几何匹配 wrong-action、反射配额和 scene bank 选择的 fail-closed 检查。
- 修复生成器的原子 shard、断点续跑和重试行为。单个 bank 失败不会静默漏掉后续 bank，resume 会重新认证已经存在的 shard。
- 在干净 formal 环境重新验证 CUDA/FP16 和双 rank NCCL。
- 修复 `CSIPairsFormalModel` 与 teacher 截断编码器的位置接口：模型改为继承 teacher 冻结的二维 position buffer，不再引用已经移除的可训练 row/column 参数。
- 整理匿名补充材料的边界。内部审计工具不进入匿名包，但匿名包自己的公开测试必须能在严格环境中完整运行。

这一阶段保留了多次失败试验和中间候选，没有删除失败日志，也没有把 V5 diagnostic 数据改成正式数据。

### 4. 8 月 15 日至 8 月 16 日：从窄带协议升级到 V6 宽带 227-bank

此前 Response/G2 的核心问题表现为 action 与 CSI 变化之间缺少足够可辨识性。继续增加训练步数没有解决这个问题，因此协议层做了有依据的升级。

#### 协议升级

- 将 CSI 从较窄的频率采样升级为 16 个子载波、1 MHz 间隔的 wideband 配置。
- 将 `source_encoder_train` 扩展为 Chicago 16 bank + Austin 16 bank，共 32 个训练 bank。
- 保留两座源城市各 4 个完全独立的 `source_method_selection` bank，共 8 个 selection bank。
- 总体协议由 203 bank 扩展为 227 bank，同时保留 41 个 source-final、82 个 target 和 32 个 external-validation bank。
- 没有降低 branching、wrong-action、null safety、copy、action-swap 或 oracle 阈值。

#### Teacher 修复

- 修复 131,072 行 teacher 计算时的内存问题。
- Transformer 推理和审计按 256 行分块，readout 用 32,768 行梯度累积，但仍保持完整数据上的 full-batch MSE 目标。
- teacher checkpoint、dataset、config、runtime、source tree 和 readout 均写入认证信息。

#### Physical response / G2 修复

- 新增 `formal_action_inverse_response.py` 和冻结的 `physical_response_v1.json`。
- 用 source-conditioned physical inverse response 替换无法通过 G2 的 pooled predictor。
- no-X 模型只读取 source CSI、source map、无 ID 的 action geometry、radio/BS 信息、mask 和 query；不读取 target CSI、target map、route label 或真实 receiver XY。
- 训练只使用 `source_encoder_train`；阈值按 fit bank 的最坏 95% 分位数校准，避免一个大 bank 掩盖弱 bank。
- 使用三成员 gate ensemble，保留完整 checkpoint 保存、加载、哈希和 round-trip 认证。
- 两个 stride-16 diagnostic 和一次全位置 diagnostic 曾达到 8/8 selection bank 通过，但这些结果明确保留为 `scientific_use=FORBIDDEN`，只能说明可以继续生成正式数据，不能填论文表格。

#### 协议冻结

- 建立 fail-closed 的 protocol-freeze 工具。
- 冻结工具要求两次不同 seed 的屏幕测试、一次完整位置测试、相同 32/8 bank 绑定、三个不同 seed、完整 checkpoint hash 和两份 RT audit 同时通过。
- 最终生成 227-bank V6 配置和 `PROTOCOL_FREEZE_PASS` receipt。

### 5. 8 月 16 日至 8 月 18 日：第一次 227-bank 完整资格链暴露真实 G2 问题

第一版完整 V6 数据位于：

```text
/root/xunlian/Futaoran/正式开始训练/CSI-PAIRS-V6-WIDEBAND-FORMAL-227-20260815T151500Z/dataset.npz
```

该 archive 的 SHA-256 是：

```text
51ae5f5b86015c1603e628c42e34beaacc51adbbd4ea46e86055969f776862ab
```

数据生成和 G1 通过后，旧 G2 正式 gate 的结果是：

- G1：`PASS`
- G2：`FAIL`
- response gate：`0/8 bank PASS`
- teacher sensitivity：`6/8 bank PASS`
- scientific use：`FORBIDDEN`

旧 gate 中 no-X 和 action-swap 实际上已经有明显改善，真正把 8 个 bank 全部判失败的是 physical oracle 几乎没有优于 copy，oracle relative improvement 大约只在 `-2.01e-4` 到 `2.99e-4` 之间。这个结果证明旧 oracle 控制没有形成论文协议要求的有效上界，不能用“训练再久一点”解释。

随后完成了两类修复：

1. teacher sensitivity 改为 source-train 冻结 readout 解码后的 normalized CSI，不再依赖容易产生自指解释的中间表征。
2. physical oracle 与 no-X 共用同一物理模型、source-only calibration 和冻结 gate，只额外使用真实 receiver XY；仍然禁止读取 target CSI 和 route label。

### 6. 8 月 19 日：完成环境重建、oracle 回归和完整测试

#### 新正式环境

创建并验证：

```text
/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/.venv-core-formal-20260819T091049Z
```

关键版本与认证信息：

| 项目 | 当前值 |
|---|---|
| Python | 3.12.13 |
| PyTorch | 2.5.1+cu121 |
| CUDA runtime | 12.1 |
| pytest | 8.4.2 |
| A100 可见数量 | 2 |
| requirements lock SHA-256 | `9548a24a4faf5db17b5251df407b4402de033ae8648ec321c850fe6105002cf1` |
| reviewed wheel manifest SHA-256 | `669aa8938bf2390c4e117240e6f7021378e5f5ff280afa2c01c48d6452d29e9d` |
| `pip check` | PASS |
| 环境内 `.pyc` | 0 |

补齐并固定了 pytest、iniconfig、packaging、pluggy、Pygments，更新了双平台 requirements lock 和精确依赖测试。没有向旧的受污染环境继续追加包。

#### Oracle 和 checkpoint 回归

新增或补强了以下测试：

- oracle 使用精确 receiver XY；
- oracle 不读取 target CSI；
- oracle 与 inferred 模式的行键严格一致；
- 无可行反射路径时返回有限零向量；
- physical checkpoint 保存、加载和哈希认证；
- checkpoint 被篡改后必须拒绝；
- teacher sensitivity 使用 source-train 冻结 readout；
- qualification gate 必须绑定当前 physical checkpoint；
- PMNet CUDA 显存检测与 adapter hash；
- DiffeRT 输入、引擎、资产、材质和外部 validity manifest 绑定。

#### 测试结果

最终完整测试：

```text
553 passed, 35 warnings in 1914.29s（31 分 54 秒）
```

同时通过：

- source tree 前后稳定性检查；
- strict runtime 零 bytecode 检查；
- 匿名补充材料递归测试；
- G8 和 claim-control adapter 源码哈希绑定测试；
- public/private atomic requirement matrix 更新后的完整性测试。

#### 诊断结果

- teacher sensitivity：`8/8 PASS`
  - active agreement 全部 `1.0`
  - null agreement 最低 `0.9951278928`
- 4-bank G2 hard pilot：`4/4 PASS`
  - 运行时间 `2154.018` 秒，约 35 分 54 秒
  - 结果明确标为 pilot，不是论文结果

PMNet 的 CUDA 内存探测和 adapter hash 已修复。DiffeRT 已扩展到 2x16 wideband CSI、四 primitive 和每 bank 16 worlds；正式 G8 目标是 32 个 external bank、512 worlds。

### 7. 8 月 19 日晚至 8 月 20 日：重新生成并完整验证当前 227-bank 数据

为了避免拿旧 source-tree 绑定的 gate 冒充当前代码资格，重新建立了全新的 run root：

```text
run id: 20260819T105509Z
data root:
/root/xunlian/Futaoran/正式开始训练/CSI-PAIRS-V6-WIDEBAND-FORMAL-227-20260819T105509Z

evidence root:
/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/artifacts/formal_readiness/formal_227bank_20260819T105509Z
```

这次生成使用 8 个 CPU/LLVM worker。227 个 bank 全部完成，启动 227 次、重试 0 次。合并后的数据在 8 月 20 日 03:01 落盘。

随后依次完成：

1. generation validation：`PASS`
2. 全文件 manifest：`PASS`
3. schema、shape、dtype、单位、坐标、NaN/Inf、零向量和重复检查：`PASS`
4. train/selection/calibration/target/external/final-unseen 隔离检查：`PASS`
5. `inspect-data`：`PASS`，`fixture=false`
6. 227/227 bank 的 live independent regeneration：`PASS`
7. 数值比较：`rtol=0`、`atol=0`
8. early bitwise bank checks：`227/227 PASS`，失败 0

当前正式资格使用的是这次新生成的数据，不是 8 月 15 日旧 archive。新 archive 信息如下：

```text
dataset.npz SHA-256:
060d8671380acaf43bd0beb2c77ac5c6ec112e4ddc083451ab36a5916b7c9cac

compressed bytes:
2,006,664,110

uncompressed array bytes:
18,202,976,564
```

旧 archive 和新 archive 都必须继续保留自己的身份和证据，不能混用 gate。当前 verifier 对新数据的 227 个 bank 做了独立重生成并逐项零容差比较，这才是当前 checkout 可接受的数据证据。

## 三、当前正式数据集统计

数据性质：**Sionna RT 模拟数据，不是真实测量 CSI。**

| 项目 | 数量 |
|---|---:|
| bank / scene / base-map cluster | 227 / 227 / 227 |
| city | 6 |
| worlds | 3,632 |
| positions | 58,112 |
| clean CSI vectors | 929,792 |
| repeated CSI vectors | 2,789,376 |
| repeat seeds | 2,789,376 |
| NPZ members | 35 |
| target support positions | Boston 5,248；Seattle 5,248 |
| NaN / Inf | 0 |
| all-zero clean / repeat vectors | 0 / 0 |
| 跨 city/role/scene 的 canonical-map 重复组 | 0 / 0 / 0 |
| 跨 city/role/scene 的 clean CSI 重复组 | 0 / 0 / 0 |

### 城市覆盖

| 城市 | bank 数 |
|---|---:|
| source-chicago | 57 |
| source-austin | 56 |
| target-boston | 41 |
| target-seattle | 41 |
| external-denver | 16 |
| external-miami | 16 |

### 角色覆盖

| 角色 | bank 数 | 用途 |
|---|---:|---|
| source_encoder_train | 32 | teacher、source normalization、物理模型 fit |
| source_method_selection | 8 | G1/G2 和方法选择，不与 train bank 重叠 |
| source_probe_train | 8 | probe 训练 |
| source_probe_selection | 8 | probe 选择 |
| source_calibration_fit | 8 | calibration fit |
| source_calibration_selection | 8 | calibration selection |
| source_final_unseen_bank | 41 | 最终 source unseen，不参与前期调参 |
| target | 82 | Boston 41 + Seattle 41 |
| external_validation | 32 | Denver 16 + Miami 16，用于外部有效性 |

数据本身已经不是当前阻塞。后续如果只修改 physical oracle/G2 代码，通常不需要再次生成 dataset；但是 source-tree hash 会变化，因此当前 verifier gate 不能直接沿用，必须按新源码重新执行绑定后的独立验证。

## 四、当前 G1/G2 结果

正式 qualification 从 8 月 20 日 10:22 运行到 13:33，实测约 3 小时 11 分。

最终 gate：

```text
status: QUALIFICATION_NO_GO
passed: false
scientific_use: FORBIDDEN
G1: PASS
G2: FAIL
```

### 8 个 selection bank 的完整结果

冻结门槛：

- no-X relative improvement vs copy：`>= 0.02`
- physical oracle relative improvement vs copy：`>= 0.10`
- action-swap：必须满足冻结 improvement 条件
- null violation rate：`<= 0.05`
- teacher active/null agreement：`>= 0.80`

| bank | no-X vs copy | vs action-swap | oracle vs copy | null violation | 结果 |
|---|---:|---:|---:|---:|---|
| Chicago-16 | 0.079993 | 0.080932 | **0.090930** | 0.002620 | FAIL |
| Chicago-17 | 0.128889 | 0.145030 | 0.147692 | 0.004914 | PASS |
| Chicago-18 | 0.101116 | 0.105105 | 0.106499 | 0.000432 | PASS |
| Chicago-19 | 0.081654 | 0.086442 | 0.112443 | 0.000676 | PASS |
| Austin-16 | 0.096425 | 0.110029 | **0.089787** | 0.013536 | FAIL |
| Austin-17 | 0.231682 | 0.241507 | 0.258802 | 0.008169 | PASS |
| Austin-18 | 0.229354 | 0.234520 | 0.224627 | 0.005165 | PASS |
| Austin-19 | 0.135591 | 0.153921 | 0.145978 | 0.006291 | PASS |

两个失败 bank 的差距：

- Chicago-16：距离 `0.10` 差 `0.009070`
- Austin-16：距离 `0.10` 差 `0.010213`

这次结果说明：

1. no-X 已经 8/8 达标。
2. action-swap 控制已 8/8 达标。
3. null safety 已 8/8 达标，最差也只有 `0.013536`，明显低于 `0.05`。
4. teacher sensitivity 已 8/8 达标。
5. 唯一剩余的 G2 问题是 oracle 在 Chicago-16 和 Austin-16 上没有达到 `0.10`。

因此不能靠降低阈值、增加训练步数或删除两个 bank 来解决。需要检查 oracle 的物理预测幅度、gate 与 receiver XY 带来的增益是否被当前校准方式压缩。

## 五、当前正在做的诊断

截至本报告生成时，正在执行一个只读的 oracle failure diagnosis，范围严格限制在两个失败的 `source_method_selection` bank：Chicago-16 和 Austin-16。

诊断内容包括：

- 对齐 inferred 和 oracle 的同一组行键；
- 分别导出 raw prediction 与 gated prediction；
- 比较 oracle/inferred 的 gate probability 分布；
- 在 active 行上扫描预测幅度系数；
- 计算 `alpha=1` 和最优 alpha 下的 NMSE 与 relative improvement；
- 判断瓶颈来自 receiver 假设、物理系数、幅度 calibration，还是 gate 把有效 oracle 响应压掉。

诊断输出目录：

```text
/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/artifacts/formal_readiness/v6_oracle_failure_diagnosis_20260820T053500Z
```

该诊断明确标记为 `DIAGNOSTIC_ONLY_NOT_PAPER_RESULT`。诊断完成前不会修改冻结阈值，也不会直接启动四臂实验。

## 六、模型、方法和代码层面已经完成的修复

### 数据和生成器

- 原子 bank shard 写入与校验；
- 确定性 bank 调度和断点续跑；
- bank 失败重试且不遗漏后续任务；
- OSM receipt、资产、场景、材质和 engine provenance 绑定；
- 四 primitive 与反射路径配额；
- visibility、roof、footprint、法向和自由空间检查；
- V6 2x16 wideband CSI；
- 完整 227-bank role ledger；
- 防止 symlink、越界路径和旧 archive replay 冒充正式生成。

### Teacher 和模型

- source-train 冻结 normalization；
- bounded chunk 推理和 readout 梯度累积；
- 固定二维 position buffer；
- 四臂 endpoint/alignment/response/full 的输入、损失和梯度路径；
- 独立 per-example/per-step 75% patch mask；
- Alignment 与 Response 梯度非零检查；
- checkpoint 的数据、配置、源码、环境和 teacher 绑定。

### G2 与控制实验

- no-X；
- copy；
- no-action；
- exact action-swap；
- physical oracle；
- wrong-map；
- shuffled-pair；
- equal-FLOP；
- mechanical concatenation；
- retention；
- scene-ID / ID-swap；
- null safety；
- source-train-frozen teacher sensitivity。

这些控制大部分已经在代码和测试层具备，但“实现存在”和“已产生正式论文结果”是两回事。当前还没有把未执行的正式 controls 写成 PASS。

### 外部模型和外部有效性

- Wi-GATr 和 PMNet 使用仓库内认证 source adapter，不要求用户提供一个来路不明的预训练 checkpoint。
- 两个模型应在正式流程中 source-only 训练，并在冻结的 source selection 上选 checkpoint。
- PMNet 已修复 CUDA 显存探测和 adapter hash。
- DiffeRT 0.10.0 的 CPU/JAX x64 runtime 已建立，许可证和 source revision 已记录。
- DiffeRT 当前代码支持 32 external bank、每 bank 16 worlds，共 512 worlds，输出 2x16 CSI。
- 旧的 128-world DiffeRT 运行只是 engineering diagnostic，不能替代当前正式 G8。

## 七、测试和复现状态

### 当前权威测试

```text
553 passed
35 warnings
1914.29 seconds
pytest exit code: 0
source-tree compare exit code: 0
bytecode check exit code: 0
```

warnings 来自 PyTorch Transformer 的 nested tensor 提示，没有 NaN、断言失败或跳过 gate。

### 环境可复现性

- 使用独立正式 venv，不修改系统 Python。
- requirements lock、reviewed wheel manifest、installer report 和 RECORD closure 均已保存。
- `pip check` 通过。
- CUDA 设备可以被 PyTorch 识别为两张 `NVIDIA A100-SXM4-40GB`。
- deterministic algorithms 开启，TF32 关闭，cuDNN deterministic 开启。
- `CUBLAS_WORKSPACE_CONFIG=:4096:8` 已写入正式链。
- 环境中没有 `.pyc`，运行时使用 `PYTHONDONTWRITEBYTECODE=1`。

### 工作区状态

当前 checkout 保持 dirty，没有执行 reset 或 checkout 覆盖已有改动。审计工作区累计有较大规模的代码、配置和测试差异，其中包括数据生成器、qualification、factorial、evaluation、teacher、external validity 和相关测试。历史 freeze 和 patch 被保留，但每次后续源码修复都会使旧 source-tree binding 失效，不能拿旧 freeze 直接给新代码背书。

## 八、GPU 和资源状态

报告时两张 GPU 的状态：

| GPU | 型号 | 已用显存 | 总显存 | 利用率 |
|---|---|---:|---:|---:|
| 0 | NVIDIA A100-SXM4-40GB | 16,814 MiB | 40,960 MiB | 约 67% |
| 1 | NVIDIA A100-SXM4-40GB | 17,397 MiB | 40,960 MiB | 约 64% |

这些占用来自当前 PID namespace 中不可见的外部进程，不是 CSI qualification 进程。按照既定要求，不会终止或干扰其他用户进程。

当前磁盘：

```text
/root: 500G total, 321G used, 180G available, 65% used
```

数据生成和独立 RT 验证主要使用 CPU，GPU 空闲并不代表任务停滞。GPU 真正需要独占的阶段是双卡 pilot、外部模型训练和四臂正式训练。

## 九、为什么现在还不能开始四臂正式实验

### 本机 P0：G2 仍未关闭

当前 qualification gate 是 `QUALIFICATION_NO_GO`，`scientific_use=FORBIDDEN`。只要 Chicago-16 和 Austin-16 的 oracle gate 没有通过，后续链就必须停止。

### G8 尚未执行

G8 watcher 原本在等待上游 formal chain。G2 失败后，post-chain exit code 为 1，DiffeRT 512-world 正式 G8 没有执行。状态文件里一度保留 `RUNNING/authenticate_chain` 是 watcher 的最后状态，不应当覆盖权威 qualification gate；实际日志已经记录上游 post-chain 未通过。

### 双卡正式数据 pilot 尚未执行

pilot 需要一个 `QUALIFICATION_PASS` gate 和准确绑定的 teacher checkpoint。当前 G2 失败，因此 pilot 被正确阻止。即使 G2 修复，两张 A100 也必须先释放到可独占状态。

### 外部 P0 仍缺失

#### C11 independent RT calibration

需要在下面目录提供真实、合法、相互独立的 fit/validation/reference 数据：

```text
/root/xunlian/Futaoran/formal_external_inputs/c11_rt_calibration/
  independent_rt_calibration.json
  protocol.json
  fit.json
  validation.json
  validation_reference.csv
  adapter.py
  source_assets/
  LICENSE_REVIEW.md
```

要求记录来源、许可证、频率、天线、坐标系、单位、材质、采集或独立引擎版本。held-out reference 不能传给 adapter。当前服务器没有这套真实外部材料。

#### C13 literature and license review

需要：

```text
/root/xunlian/Futaoran/formal_external_inputs/c13_literature/
  literature_resources.json
  receipts/
  papers/
  HUMAN_REVIEW.md
```

Crossref 和 OpenAlex 已有检索 receipt；Semantic Scholar 最后一次请求仍是 HTTP 429。即使机器检索补齐，也必须由真实人员完成 novelty、代码、数据、模型许可证和再分发限制审阅，不能由程序把 `licenses_reviewed` 自动写成 true。

#### Human approval manifest

`prepare-full-run` 通过后会生成与当前 dataset、config、source tree、environment、gate 和资源计划精确绑定的 approval request。只有真实审批者签发对应 manifest 后才能运行 `all`。旧 approval 或手工伪造 JSON 都不能使用。

## 十、距离四臂正式实验还剩哪些步骤

按顺序必须完成：

1. 完成当前两个失败 bank 的 oracle 诊断。
2. 如果确认是实现或校准问题，修复代码并增加针对性回归测试。
3. 在新的输出目录运行真实 G2 pilot，先确认 8/8 bank 全过。
4. 因源码 hash 会变化，重新运行当前 227-bank 数据的 source-bound 独立验证。
5. 在新的 qualification root 完整重跑 G1/G2，要求 `G1=PASS`、`G2=PASS`、`scientific_use=FORMAL_EXPERIMENT_ALLOWED`。
6. 两张 A100 空闲后运行正式数据双卡 pilot，验证 AMP、NCCL、loss、梯度、NaN/Inf、checkpoint、resume、指标导出、吞吐和显存。
7. 为当前 V6 数据生成 DiffeRT 512-world 包并执行正式 G8。
8. 正式执行 Wi-GATr、PMNet、simple baseline、wrong-map、shuffled-pair、retention 和 resource controls。
9. 用真实材料关闭 C11。
10. 用完整 receipts 和人工审阅关闭 C13。
11. 根据双卡 pilot 的实测吞吐生成 compute plan。
12. 运行 `prepare-full-run`，取得精确绑定的真实 human approval manifest。
13. 确认两张 A100 可独占、磁盘足够、输出目录全新。
14. 启动四臂正式实验：endpoint、alignment、response、full。

正式配置保持：

```text
seeds: 20270001, 20270002, 20270003
arms: endpoint, alignment, response, full
steps per arm per seed: 20000
batch size: 128
devices: cuda:0, cuda:1
```

## 十一、剩余时间如何估算

下面只采用这台机器已经测到的时间，不用经验拍一个乐观数字。

| 环节 | 当前实测 |
|---|---:|
| 完整 pytest | 31 分 54 秒 |
| 4-bank G2 hard pilot | 35 分 54 秒 |
| 当前 227-bank 独立复验 | 约 6 小时 36 分 |
| 当前完整 qualification | 约 3 小时 11 分 |
| 旧 DiffeRT 128-world diagnostic | 19 分 14 秒，仅作工程参考，不能直接当 512-world G8 ETA |

如果 oracle 修复第一次就正确，已知必须重跑的硬时间下限约为：

```text
完整测试             0.53 h
真实 G2 pilot        0.60 h
227-bank 独立复验    6.60 h
完整 G1/G2           3.18 h
--------------------------------
已知小计            10.91 h
```

这个小计还没有包含：

- oracle 根因定位和代码修改时间；
- DiffeRT 512-world 正式 G8；
- 双卡正式数据 pilot；
- PMNet/Wi-GATr 和其他控制实验；
- GPU 等待时间；
- C11、C13 和人工 approval 的外部等待时间。

因此，如果只看本机可解决部分、修复一次成功且 GPU 及时释放，现实下限约为 12 至 16 小时。这个时间不是四臂正式开跑承诺。外部 C11/C13/approval 未提供前，四臂实验没有一个可以诚实承诺的确定启动时间。

## 十二、当前权威证据位置

### 当前数据与验证

```text
数据：
/root/xunlian/Futaoran/正式开始训练/CSI-PAIRS-V6-WIDEBAND-FORMAL-227-20260819T105509Z/dataset.npz

数据 manifest：
/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/artifacts/formal_readiness/formal_227bank_20260819T105509Z/dataset_manifest/DATASET_MANIFEST.json

数据契约：
/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/artifacts/formal_readiness/formal_227bank_20260819T105509Z/inspect/data_contract.json

独立复验 gate：
/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/artifacts/formal_readiness/formal_227bank_20260819T105509Z/verification_and_qualification/data_verification/gate.json
```

### 当前 G1/G2

```text
qualification gate：
/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/artifacts/formal_readiness/formal_227bank_20260819T105509Z/verification_and_qualification/qualification/gate.json

8-bank response gate：
/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/artifacts/formal_readiness/formal_227bank_20260819T105509Z/verification_and_qualification/qualification/response_gate.csv

teacher sensitivity：
/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/artifacts/formal_readiness/formal_227bank_20260819T105509Z/verification_and_qualification/qualification/teacher_qualification.csv
```

### 测试与环境

```text
完整测试：
/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/artifacts/formal_readiness/phase1-full-pytest-stable-final-20260819T182900Z/pytest.log

正式环境证据：
/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/artifacts/formal_readiness/formal-env-pytest-rebuild-20260819T091049Z

外部输入契约：
/root/xunlian/Futaoran/CSI_CLOUD_LATEST_3183664/code/CSI-PAIRS-v2.0-server/artifacts/formal_readiness/EXTERNAL_INPUT_CONTRACTS.md
```

## 十三、最终判断

这两周已经解决了环境污染、依赖不可复现、GPU/NCCL 验证、数据规模不足、窄带 response 不可辨识、场景几何与资产绑定、teacher 大批量内存、checkpoint 认证、指标实现、统计单位、数据泄漏、旧 oracle 无效、teacher sensitivity 不稳定、PMNet CUDA 探测和 DiffeRT wideband 接口等一系列问题。

当前最重要的进展不是“已经开始训练”，而是已经拿到了一条可信的失败结论：当前数据本身通过，G1 通过，G2 的 no-X、action-swap、null safety 和 teacher sensitivity 全部通过，只剩两个 bank 的 oracle improvement 各差约 1 个百分点。这比旧版本 8 个 bank 全部失败已经明显收敛，但仍然不能越过门槛。

下一步只围绕这两个 oracle bank 做根因修复。修复后必须重新测试、pilot、source-bound 数据复验和完整 qualification。只有 G2、双卡 pilot、G8、C11、C13 和真实 approval 全部通过，才会启动 endpoint、alignment、response、full 四臂正式实验。
